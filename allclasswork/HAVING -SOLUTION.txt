1-
SELECT COUNT(*),DEPTNO
FROM EMP
WHERE JOB='CLERK'
GROUP BY DEPTNO
HAVING COUNT(*)>=2;



3-
 SELECT COUNT(*),SUM(SAL),JOB
 FROM EMP
 WHERE SAL>1200
 GROUP BY JOB
 HAVING SUM(SAL)>3800;
4-
 SELECT COUNT(*),DEPTNO
 FROM EMP
 WHERE JOB='MANAGER'
 GROUP BY DEPTNO
 HAVING COUNT(*)=2;
5- 
SELECT JOB,MAX(SAL)
 FROM EMP
 GROUP BY JOB
 HAVING MAX(SAL)>1600;
6-
SELECT COUNT(*),SAL
FROM EMP
GROUP BY SAL
HAVING COUNT(*)>1;
7-
SELECT COUNT(*),HIREDATE
FROM EMP
GROUP BY HIREDATE
HAVING COUNT(*)>1;
8-
SELECT AVG(SAL),DEPTNO
FROM EMP
GROUP BY DEPTNO
HAVING AVG(SAL)<3000;
9-
SELECT DEPTNO
FROM EMP
WHERE ENAME LIKE '%A%' OR ENAME LIKE '%S%'
GROUP BY DEPTNO
HAVING COUNT(*)>=3;
10-
SELECT MAX(SAL),MIN(SAL),JOB,COUNT(*)
FROM EMP
GROUP BY JOB
HAVING MIN(SAL)>1000 AND MAX(SAL)<5000;

